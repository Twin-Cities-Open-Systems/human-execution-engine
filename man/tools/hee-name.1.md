# hee-name(1)

```
usage: hee-name [-h] [-list-pools] [-allocate] [-release NAME] [-list]
                [--pool POOL] [--scope SCOPE] [--prefix PREFIX]

options:
  -h, --help       show this help message and exit
  -list-pools
  -allocate
  -release NAME
  -list
  --pool POOL
  --scope SCOPE
  --prefix PREFIX
```
