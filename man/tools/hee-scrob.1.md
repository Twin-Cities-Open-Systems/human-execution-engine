# hee-scrob(1)

```
usage: hee-scrob [-h] [-server SERVER] [-current-only] [-discord]

options:
  -h, --help      show this help message and exit
  -server SERVER
  -current-only   only the exact-moment subtitle line, no before/after context
  -discord        post to Discord (webhook URL from $HEE_CRED_PASS) instead of
                  stdout
```
