# hee-scrob(1)

```
usage: hee-scrob [-h] [-server SERVER] [-current-only]

options:
  -h, --help      show this help message and exit
  -server SERVER
  -current-only   only the exact-moment subtitle line, no before/after context
```
