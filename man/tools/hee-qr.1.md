# hee-qr(1)

```
usage: hee-qr [-h] [-manifest PATH] image

positional arguments:
  image

options:
  -h, --help      show this help message and exit
  -manifest PATH  write/update a static {recipe_id: recipe} JSON manifest
                  entry
```
