.\" Automatically generated by Pandoc 3.1.3
.\"
.\" Define V font for inline verbatim, using C font in formats
.\" that render this, and otherwise B font.
.ie "\f[CB]x\f[]"x" \{\
. ftr V B
. ftr VI BI
. ftr VB B
. ftr VBI BI
.\}
.el \{\
. ftr V CR
. ftr VI CI
. ftr VB CB
. ftr VBI CBI
.\}
.TH "" "" "" "" ""
.hy
.SH hee-reset-tooling(1)
.PP
section: 1 hee-reset-tooling -- real, conservative \[dq]reset to
new\[dq] for stray tool directories (real trigger, Spencer 2026-08-24:
fleet-ops#260\[aq]s stray \[ti]/tooling/bin/ cleanup, formalized into a
reusable tool instead of a one-off manual pass).
.PP
Real safety, per Spencer\[aq]s explicit call: - Dry-run by default.
Nothing is ever removed without --yes -- an OPER must actually agree,
not just invoke the command.
- --unattended (cron-shaped invocation) additionally requires the real
environment to be idle first (see is_idle below) -- refuses to run
destructive cleanup while a real user might be mid-work, checks
who\[aq]s actually logged in and how long they\[aq]ve been idle, not
just \[dq]no tty attached.\[dq] - NEVER touches: \[ti]/.ssh,
\[ti]/.gnupg, \[ti]/.hee/secrets, \[ti]/.config, or anything under
\[ti]/git/ -- refuses outright if a stray path resolves inside one of
those.
- A stray file is only removed if it\[aq]s byte-identical to the real
tracked equivalent; anything that differs, or has no tracked equivalent,
gets backed up first, never silently destroyed.
.PP
Usage: hee-reset-tooling [--yes] [--unattended] [--idle-mins N]
.PD 0
.P
.PD
[--canonical DIR] [--stray DIR ...]
(defaults: canonical = \[ti]/git/human-execution-engine/tooling/bin,
stray = \[ti]/tooling/bin \[ti]/bin, idle-mins = 10)
.PP
\f[I](no --help/-h output -- generated from the script\[aq]s own header
comment)\f[R]
