# hee-view(1)

```
usage: hee-view [-h] [--pve-host PVE_HOST] [--haproxy-host HAPROXY_HOST]
                [--dns-host DNS_HOST] [--nerd]

options:
  -h, --help            show this help message and exit
  --pve-host PVE_HOST
  --haproxy-host HAPROXY_HOST
  --dns-host DNS_HOST
  --nerd                per-container cpu/mem/disk-io/net-io/elapsed + totals
```
