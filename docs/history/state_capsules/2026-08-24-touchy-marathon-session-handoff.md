# Handoff capsule: touchy-claude marathon session, 2026-08-24

**Purpose**: if the current session (Claude, running as `touchy-claude` on
`kiosk.lab.tcos.us`) runs out of usable budget, this is what any
replacement model — Claude or otherwise — needs to pick up cold, fast.
Non-authoritative per `docs/STATE_CAPSULE_GUIDE.md` — verify against real
state (git log, `gh issue/pr list`) before trusting anything below as
still current.

## Who you are, in this context

You're operating as `touchy-claude`, a real GitHub identity in the
Twin-Cities-Open-Systems (TCOS) org, on host `kiosk.lab.tcos.us`, working
directly with **Spencer Butler** (the human, sole authority, `spencer` on
this host — real, unprivileged by design, never sudo). `gh auth status`
confirms which account you're actually running as — check it first.

## Where the real context lives (read these, in order)

1. `docs/doctrine/HEE.md` + `docs/doctrine/HEE_POLICY.md` — the formal doctrine.
2. `docs/guides/HEE_EXPLAINED.md` — same thing, plain-language (read this first if pressed for time).
3. `docs/doctrine/DECISION_FRAMEWORK.md` — the real decision-making rules this session actually used, with real examples.
4. `CORPUS.md` (this repo and `.github`) — "what actually exists here" index.
5. This session's memory (Claude-specific, may not be readable by another model): `/home/claude/.claude/projects/-home-claude/memory/MEMORY.md` — real, curated facts about Spencer, the org, and standing feedback/policy. If your harness can read local files, read this.

## Real, current org map

- `human-execution-engine` (public) — doctrine/tooling, most of tonight's real work.
- `fleet-ops` (private) — ops/incident/governance tracking, most real tickets live here.
- `.github` (public) — org profile + glossary (`profile/GLOSSARY.md`).
- `tcos-www` (public) — the public site, generated by `generate-public-site.py`.
- `tcos-plan-private` (private) — real, sensitive strategic/personal content. Treat `hee/evidence/` there as untouchable without explicit direction.
- `primitives` — tool-provenance tracker, schema just landed (PR#6).
- `resume` — the blog/resume publish pipeline.

## Tonight's real work, condensed

- Recovered + genericized real doctrine from `tcos-plan-private` into public HEE (HEE#346, merged) — opus/corpus glossary terms, RegNs Kind, treasury/kinship/foundation governance patterns.
- Built a real GitHub rate-limit crisis-response chain: `hee-quota` (monitor), `hee-filter` (shared publish-safety gate — repo-visibility check + content scan via `tcos-audit`), GraphQL call-count sqz via mutation aliasing in `hee-fields`/`hee-git-merge`. All real, shipped, fleet-ops#269 (closed, superseded by follow-ups).
- Found + fixed a real security gap in `hee-publish` (PR#331, still open) before merge: no repo-visibility filter, would have leaked private-repo activity onto a public blog.
- Wrote `contracts/publish-sanitization-v1.contract.yaml` — the real, durable policy mandate behind all of the above.
- Full-org field backfill (HEE#341): 258 open + 53 closed issues audited; batches 1-2 of 4 done (60 items); batches 3-4 (150 items) queued, not yet run.
- `primitives.yaml`/`external.yaml`/`migrating.yaml`/`own-tools.yaml` real entry schema shipped (primitives#6).
- CFO/value-definition report posted (HEE#306) — honest finding: TCOS's Rule of 40 is undefined (no revenue baseline), not "low."
- Filed HEE#352: local LLM (Ollama/llama.cpp) for narrow predicate-gate evaluation — separate problem from GitHub API throttling, addresses Claude usage-budget pressure specifically. **This capsule is a direct response to that same pressure.**
- Real pve (Proxmox, 10.0.0.153) recon done tonight: 0 VMs (clean slate per fleet-ops#97), zero ZFS pools despite fleet-ops#219's root-on-ZFS plan, 2 SSDs + 2 HDDs unallocated. Relayed to kenny, who's drafting the real buildout plan.

## Live, concurrent peers (as of this capsule)

- **kenny** — active peer on this same host, drafting the pve buildout plan, also shipped a real disk-cache fix for `hee-git-merge` (PR#349).
- **claude-f2** — another active peer, real work in progress, cross-session messages currently gated behind per-recipient approval (a real, working safety mechanism, not a bug).
- Cross-session messaging works via `SendMessage`/`ListAgents` if your harness supports it; if not, Spencer is the real relay between sessions.

## Standing rules that matter most, distilled

- **Verify, never trust a self-report** — your own tool's success message, a prior claim, a stale doc. Re-check against real state.
- **Real precedent over invention** — search before building; this session repeatedly found real existing tickets/tools instead of duplicating.
- **Match caution to blast radius** — low-stakes (open a PR, write a doc) proceeds directly; broad/irreversible (merges, org-wide changes, anything financial) stops and asks.
- **Findings are durable, not chat-ephemeral** — file real tickets/comments, cross-link both directions.
- **`spencer@kiosk` is permanently unprivileged by design** — never propose sudo for it.
- **Content going public gets scanned first** — `hee-filter scan`, mandatory, fails closed.

## Immediate next real steps, unstarted

1. Batches 3-4 of the field backfill (150 items, HEE#341) — files already built, at `/home/claude/.claude/jobs/ae94b352/tmp/open-batches/batch{3,4}.jsonl` (session-local, may not survive a session change — regenerate from `full_backfill_state.json` if gone).
2. `resume/convert.sh` — not yet wired to `hee-filter check-mode` for language-profile safety.
3. PR#331 (`hee-publish`) still needs Spencer's merge approval.
4. HEE#352 (local LLM) — design-only so far, no implementation.

If you're a fresh model reading this cold: read `HEE_EXPLAINED.md` first,
then `gh pr list`/`gh issue list --assignee touchy-claude` across the
repos above for real current state, then ask Spencer what's most urgent
rather than guessing.
