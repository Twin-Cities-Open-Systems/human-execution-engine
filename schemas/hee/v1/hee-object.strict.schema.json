{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "HEE Object (strict)",
  "description": "Strict overlay: requires metadata.annotations.inuid to be present. Well-formedness of inuid itself (string vs. null, and the null+inuid_null_reason coupling) is governed by hee-object.schema.json (base) so there is exactly one place that rule lives -- strict only adds the presence requirement on top.",
  "allOf": [
    { "$ref": "hee-object.schema.json" },
    {
      "type": "object",
      "required": ["metadata"],
      "properties": {
        "metadata": {
          "type": "object",
          "required": ["annotations"],
          "properties": {
            "annotations": {
              "type": "object",
              "required": ["inuid"]
            }
          }
        }
      }
    }
  ]
}
